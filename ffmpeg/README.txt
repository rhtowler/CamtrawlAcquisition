
Windows users can copy ffmpeg here and set ffmpeg_path in the config file to:

    ffmpeg_path: ./ffmpeg/bin


Linux users will most likely want to use the system ffmpeg and should check
the config file to ensure the ffmpeg_path parameter is empty:

    ffmpeg_path: 